- name: Create MetalLB Namespace
  kubernetes.core.k8s:
    state: present
    api_version: v1
    kind: Namespace
    name: metallb
    metadata:
      labels:
        pod-security.kubernetes.io/enforce: privileged
        pod-security.kubernetes.io/audit: privileged
        pod-security.kubernetes.io/warn: privileged

- name: Add MetalLB Chart Repo
  kubernetes.core.helm_repository:
    name: metallb
    repo_url: https://metallb.github.io/metallb

- name: Deploy Latest MetalLB
  kubernetes.core.helm:
    name: metallb
    chart_ref: metallb/metallb
    release_namespace: metallb
    create_namespace: true

- name: Deploy MetalLB Resources - L2 Advertisement
  kubernetes.core.k8s:
    state: present
    api_version: metallb.io/v1beta1
    kind: L2Advertisement
    name: l2
    namespace: metallb
    
- name: Deploy MetalLB Resources - IP Address Pool
  kubernetes.core.k8s:
    state: present
    api_version: metallb.io/v1beta1
    kind: IPAddressPool
    name: default
    namespace: metallb
    definition:
      spec:
        addresses:
          - 10.0.15.0/24

# > ## PASS/FAIL: LoadBalancer External IP
# > Install nginx with an external IP
# > - `kubectl apply -f infrastructure/test-nginx.yaml`
# > - `watch kubectl get svc -n default`

