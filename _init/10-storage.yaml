##### STORAGE ##################################################################
#
# kubectl config set-context --current --namespace=vault
# helm repo add nfs-subdir-external-provisioner https://kubernetes-sigs.github.io/nfs-subdir-external-provisioner/
# helm install glass-bulk-provisioner nfs-subdir-external-provisioner/nfs-subdir-external-provisioner --values=infrastructure/storage/bulk-values.yaml
# helm install glass-cfg-provisioner nfs-subdir-external-provisioner/nfs-subdir-external-provisioner --values=infrastructure/storage/cfg-values.yaml
# 
##### STORAGE ##################################################################
